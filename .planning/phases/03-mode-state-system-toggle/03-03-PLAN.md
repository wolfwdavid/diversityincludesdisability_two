---
phase: 03-mode-state-system-toggle
plan: 03
type: tdd
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/lib/components/ModeToggle.svelte
  - src/lib/components/ModeToggle.svelte.spec.ts
  - src/routes/+layout.svelte
  - tests/mode.e2e.ts
autonomous: true
requirements: [MODE-01, MODE-05, MODE-06]

must_haves:
  truths:
    - "A header switch with role='switch' + aria-checked reflects the current mode and flips it on click (MODE-01/06)"
    - "Enter and Space activate the switch (native button) and toggle aria-checked (MODE-06)"
    - "Focus stays on the switch after toggling — it is not moved to body or elsewhere (MODE-05)"
    - "A visually-hidden aria-live='polite' region announces the new mode on each toggle (MODE-05)"
    - "The switch shows a visible focus state via the existing :focus-visible outline token (MODE-06)"
    - "Choice persists across a full page reload; data-mode is correct on first paint after reload (MODE-01/02/04)"
    - "Scroll position is preserved across a toggle (no navigation) (MODE-05)"
  artifacts:
    - path: "src/lib/components/ModeToggle.svelte"
      provides: "Native <button role='switch' aria-checked> driving toggleMode() + colocated aria-live region"
      contains: "role=\"switch\""
    - path: "src/lib/components/ModeToggle.svelte.spec.ts"
      provides: "Client(browser) ARIA/keyboard/live-region/focus spec"
      min_lines: 30
    - path: "src/routes/+layout.svelte"
      provides: "Minimal <header> hosting <ModeToggle/> + data-mode-ready unlock on mount"
      contains: "ModeToggle"
    - path: "tests/mode.e2e.ts"
      provides: "Playwright: persistence-across-reload + no-flash + scroll-preserved"
      min_lines: 25
  key_links:
    - from: "src/lib/components/ModeToggle.svelte"
      to: "src/lib/mode/mode.svelte.ts"
      via: "import { isPremium, toggleMode }"
      pattern: "from '\\$lib/mode/mode.svelte'"
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/ModeToggle.svelte"
      via: "import + render in <header>"
      pattern: "ModeToggle"
---

<objective>
Build the user-facing switch and wire it into the layout, then prove the end-to-end behaviors that only a
real browser reload can verify. The `ModeToggle.svelte` is a native `<button role="switch" aria-checked>`
(Enter/Space free) that drives the Plan-02 store, colocated with a visually-hidden `aria-live="polite"`
region that announces the mode change. `+layout.svelte` gains a minimal `<header>` slot to host it (Phase 4
builds the full nav around this) plus the `requestAnimationFrame` `data-mode-ready` unlock. A Playwright spec
covers persistence-across-reload, no-flash-on-first-paint, and scroll-preservation.

Purpose: Completes MODE-01 (header toggle), MODE-05 (announce + focus + scroll), MODE-06 (switch semantics +
keyboard + visible focus), and gives MODE-04 its faithful real-browser check.

Output: `src/lib/components/{ModeToggle.svelte, ModeToggle.svelte.spec.ts}`; edited `src/routes/+layout.svelte`; `tests/mode.e2e.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-mode-state-system-toggle/03-RESEARCH.md
@.planning/phases/03-mode-state-system-toggle/03-VALIDATION.md
@.planning/phases/03-mode-state-system-toggle/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (src/lib/mode/mode.svelte.ts): -->
<!--   isPremium(): boolean; toggleMode(): Mode; getMode(): Mode; setMode(next: Mode): void; -->
<!-- Existing +layout.svelte (7 lines): imports app.css + favicon, `let { children } = $props();`, -->
<!--   renders <svelte:head><link icon></svelte:head> then {@render children()}. Preserve all of this. -->
<!-- app.css already provides: .visually-hidden, :focus-visible outline, .mode-toggle__track no-transition -->
<!--   guard, [data-mode] hooks (from Plan 02). Reuse .visually-hidden + .mode-toggle__track class names. -->
<!-- Content barrel ($lib/content): source any user-facing label from here if a string exists; the toggle -->
<!--   accessible name 'Premium 3D mode' is a control label (no content-barrel entry required in Phase 3). -->
<!-- adapter-static base path: preview serves the site under /diversityincludesdisability_two/ (trailingSlash:'always'). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModeToggle switch component with ARIA/keyboard/live-region (RED→GREEN)</name>
  <files>src/lib/components/ModeToggle.svelte, src/lib/components/ModeToggle.svelte.spec.ts</files>
  <read_first>
    - .planning/phases/03-mode-state-system-toggle/03-RESEARCH.md (Pattern 5 switch control; switch-vs-toggle table; WCAG 2.2 AA mapping incl. 2.5.8 target size ≥24px; Anti-Patterns: no focus move, no transition-on-load)
    - src/lib/mode/mode.svelte.ts (isPremium, toggleMode — from Plan 02)
    - src/lib/vitest-examples/Welcome.svelte.spec.ts (client spec pattern: render from vitest-browser-svelte, page.getByRole)
    - src/app.css (.visually-hidden, .mode-toggle__track, :focus-visible — reuse these class names)
  </read_first>
  <behavior>
    ModeToggle.svelte.spec.ts (CLIENT/chromium project) asserts:
    - renders a control with role="switch" and an accessible name containing "Premium 3D mode"
    - aria-checked reflects isPremium(): after setMode('premium') → aria-checked="true"; after 'accessible' → "false"
    - clicking the switch flips aria-checked AND flips document.documentElement data-mode
    - keyboard: focusing the button and pressing Enter toggles; pressing Space toggles (native button behavior)
    - after a toggle, document.activeElement is still the switch button (focus NOT moved — MODE-05)
    - a visually-hidden element with aria-live="polite" updates its text to "Premium mode enabled" /
      "Accessible mode enabled" after toggling (MODE-05 announcement)
    beforeEach: localStorage.clear(); reset data-mode to 'accessible'.
  </behavior>
  <action>
    Create `src/lib/components/ModeToggle.svelte` (runes; NO three/threlte import):
      <script lang="ts">
        import { isPremium, toggleMode } from '$lib/mode/mode.svelte';
        let announce = $state('');
        function onToggle() {
          const next = toggleMode();
          announce = next === 'premium' ? 'Premium mode enabled' : 'Accessible mode enabled';
          // focus intentionally NOT moved — stays on this button (MODE-05)
        }
      </script>

      <button
        type="button"
        role="switch"
        aria-checked={isPremium()}
        onclick={onToggle}
        class="mode-toggle"
      >
        <span class="mode-toggle__label">Premium 3D mode</span>
        <span class="mode-toggle__track" aria-hidden="true"></span>
      </button>
      <span class="visually-hidden" aria-live="polite">{announce}</span>

    Add a scoped <style> giving `.mode-toggle` a min target size of 24×24 CSS px (WCAG 2.5.8) and the
    `.mode-toggle__track` visual (transition allowed only when motion is fine — the no-transition-until-ready
    + reduced-motion guards already live in app.css from Plan 02; do NOT re-add !important overrides here).
    Do NOT set outline:none — the global :focus-visible ring is the visible focus state (MODE-06).

    Create `src/lib/components/ModeToggle.svelte.spec.ts` (client project) per the behavior block, using
    `render` from 'vitest-browser-svelte' and `page` from 'vitest/browser'; drive keyboard via the browser
    API (focus the button, dispatch Enter/Space). TDD order: spec first (RED), component second (GREEN).
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run --project client src/lib/components/ModeToggle.svelte.spec.ts` exits 0
    - `grep -q 'role="switch"' src/lib/components/ModeToggle.svelte` succeeds
    - `grep -q 'aria-checked={isPremium()}' src/lib/components/ModeToggle.svelte` succeeds
    - `grep -q 'aria-live="polite"' src/lib/components/ModeToggle.svelte` succeeds
    - `grep -q "from '\$lib/mode/mode.svelte'" src/lib/components/ModeToggle.svelte` succeeds
    - `grep -c "outline: *none\|outline:none" src/lib/components/ModeToggle.svelte` returns 0 (focus ring preserved)
    - no `three`/`@threlte` import in the component
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project client src/lib/components/ModeToggle.svelte.spec.ts</automated>
  </verify>
  <done>Component spec GREEN: switch semantics, keyboard, focus-stays, and aria-live announcement all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ModeToggle in a header slot + data-mode-ready unlock in +layout</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>
    - src/routes/+layout.svelte (current 12 lines — preserve app.css import, favicon, children render)
    - .planning/phases/03-mode-state-system-toggle/03-RESEARCH.md (Open Q#3 header slot; Code Examples: mount-time data-mode-ready unlock via requestAnimationFrame)
    - src/lib/components/ModeToggle.svelte (from Task 1)
  </read_first>
  <action>
    Edit `src/routes/+layout.svelte` (keep the existing `import '../app.css'`, favicon link, and
    `{@render children()}`):
    1. Import ModeToggle: `import ModeToggle from '$lib/components/ModeToggle.svelte';`
    2. Add `import { onMount } from 'svelte';` and an onMount that unlocks transitions after first paint:
       `onMount(() => requestAnimationFrame(() => document.documentElement.setAttribute('data-mode-ready', '')));`
    3. Render a MINIMAL header hosting the toggle ABOVE the page content:
       `<header class="site-header"><ModeToggle /></header>`
       Do NOT build full navigation here — Phase 4 fleshes out the nav around this slot (LOCKED decision #6).
    Keep it to a header + the toggle; no nav links, no logo work.
  </action>
  <acceptance_criteria>
    - `grep -q "ModeToggle" src/routes/+layout.svelte` succeeds
    - `grep -q "data-mode-ready" src/routes/+layout.svelte` succeeds
    - `grep -q "requestAnimationFrame" src/routes/+layout.svelte` succeeds
    - `grep -q "import '../app.css'" src/routes/+layout.svelte` still succeeds (existing import preserved)
    - `grep -q "@render children()" src/routes/+layout.svelte` still succeeds (page content still rendered)
    - `<header` element present (`grep -q "<header" src/routes/+layout.svelte`)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>+layout renders a header-hosted ModeToggle and stamps data-mode-ready on mount; existing layout behavior intact.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E — persistence, no-flash, scroll-preserved</name>
  <files>tests/mode.e2e.ts</files>
  <read_first>
    - playwright.config.ts (testMatch '**/*.e2e.{ts,js}', webServer builds+previews on port 4173)
    - .planning/phases/03-mode-state-system-toggle/03-RESEARCH.md (Pitfall 1 FOUC check; Pitfall 4 scroll/focus; Validation Architecture E2E rows)
    - svelte.config.js / vite.config.ts (base path — preview serves under /diversityincludesdisability_two/)
  </read_first>
  <action>
    Create `tests/mode.e2e.ts` using `@playwright/test`. Base URL is not configured, so navigate to the
    absolute preview path `http://localhost:4173/diversityincludesdisability_two/` (trailingSlash:'always').
    Write three tests:
    1. PERSISTENCE (MODE-01/02): goto home; click the `[role="switch"]`; read the resulting
       `document.documentElement.dataset.mode`; reload the page; assert `dataset.mode` on first evaluation
       after reload equals the value chosen before reload (choice survived via localStorage 'did2:mode').
    2. NO-FLASH (MODE-04): set localStorage `did2:mode='premium'` via `page.addInitScript`, then goto home;
       immediately assert `document.documentElement.getAttribute('data-mode') === 'premium'` (the inline head
       script stamped it before paint — no accessible→premium flip).
    3. SCROLL-PRESERVED (MODE-05): goto a page tall enough to scroll (or set a large window/scroll); scroll to
       a known y (`window.scrollTo(0, 300)`); click the switch; assert `window.scrollY` is unchanged (300)
       and `document.activeElement` matches the switch (focus not moved).
    Use `page.locator('[role="switch"]')`. Keep selectors role-based, not brittle CSS.
  </action>
  <acceptance_criteria>
    - `tests/mode.e2e.ts` matches the configured testMatch `**/*.e2e.ts`
    - file contains three `test(` blocks covering persistence, no-flash, and scroll
    - `grep -q 'role="switch"' tests/mode.e2e.ts` succeeds
    - `grep -q "did2:mode" tests/mode.e2e.ts` succeeds (asserts the namespaced key, via addInitScript)
    - `grep -q "diversityincludesdisability_two" tests/mode.e2e.ts` succeeds (base-path-aware URL)
    - `pnpm exec playwright test tests/mode.e2e.ts` exits 0 (builds, previews, all three pass)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/mode.e2e.ts</automated>
  </verify>
  <done>E2E proves choice persists across reload, mode is correct on first paint (no flash), and scroll+focus survive a toggle.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run` GREEN across both projects (server logic + client ARIA/keyboard/store).
- `pnpm exec playwright test tests/mode.e2e.ts` GREEN (persistence + no-flash + scroll).
- `pnpm check` exit 0 (types + svelte a11y).
- Manual residuals (deferred to /gsd:verify-work): NVDA/VoiceOver announcement of the aria-live region.
</verification>

<success_criteria>
The header switch flips the whole site's data-mode with correct switch semantics, keyboard operation, visible
focus, an aria-live announcement, preserved focus+scroll, and a choice that persists across reloads with no
flash on first paint — satisfying all five Phase-3 roadmap success criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mode-state-system-toggle/03-03-SUMMARY.md`.
</output>
