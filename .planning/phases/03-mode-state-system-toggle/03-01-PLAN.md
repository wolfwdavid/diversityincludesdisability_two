---
phase: 03-mode-state-system-toggle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - src/lib/mode/constants.ts
  - src/lib/mode/resolve.ts
  - src/lib/mode/resolve.spec.ts
autonomous: true
requirements: [MODE-02, MODE-03, MODE-07]

must_haves:
  truths:
    - "Given no stored choice and prefers-reduced-motion:reduce, resolveMode returns 'accessible' (MODE-03)"
    - "Given no stored choice and WebGL unavailable, resolveMode returns 'accessible' (MODE-07)"
    - "Given a valid stored choice, resolveMode returns it regardless of other signals (MODE-02 precedence)"
    - "Given a capable, no-preference, no-stored visitor, resolveMode returns 'premium' (locked default)"
    - "Invalid/garbage stored values are ignored, not trusted"
    - "The localStorage key is the namespaced string 'did2:mode' in exactly one shared source of truth"
    - "playwright.config.ts webServer runs preview with BASE_PATH=/diversityincludesdisability_two so the Plan-03 E2E resolves the production subpath (not a 404 at root)"
  artifacts:
    - path: "src/lib/mode/constants.ts"
      provides: "STORAGE_KEY='did2:mode', DATA_ATTR='data-mode', MODES tuple, Mode type — the single shared constants surface"
      contains: "did2:mode"
    - path: "src/lib/mode/resolve.ts"
      provides: "Pure resolveMode(signals) precedence function + hasWebGL() probe — the single precedence source of truth"
      exports: ["resolveMode", "hasWebGL", "ModeSignals"]
    - path: "src/lib/mode/resolve.spec.ts"
      provides: "6-row precedence truth table (server/node project)"
      min_lines: 25
  key_links:
    - from: "src/lib/mode/resolve.ts"
      to: "src/lib/mode/constants.ts"
      via: "import { STORAGE_KEY, type Mode }"
      pattern: "from './constants'"
---

<objective>
Build the precedence source of truth for the whole mode system: a pure, unit-tested `resolveMode()`
function whose truth table encodes the LOCKED precedence `stored > reduced-motion > no-WebGL > premium`,
plus the shared `constants.ts` (namespaced `did2:mode` key). Also fix the pnpm/npm tooling drift so the
test + e2e commands run consistently.

Purpose: Every other artifact in Phase 3 (the runes store, the inline no-flash head script, the toggle)
either imports these constants or mirrors this precedence. Locking it first, with a fast node-project
truth-table spec, makes MODE-02/03/07 verifiable in sub-second runs and prevents drift downstream.

Output: `src/lib/mode/{constants,resolve,resolve.spec}.ts`; corrected `package.json` + `playwright.config.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mode-state-system-toggle/03-RESEARCH.md
@.planning/phases/03-mode-state-system-toggle/03-VALIDATION.md

<interfaces>
<!-- House style from Phase 2 (src/lib/content/*): readonly types, `as const`, per-invariant spec files. -->
<!-- Existing spec pattern (src/lib/content/nav.spec.ts): import { describe, it, expect } from 'vitest'. -->
<!-- Vitest 2-project split (vite.config.ts): server = node env, include src/**/*.{test,spec}.{js,ts}, -->
<!--   EXCLUDING *.svelte.{test,spec}.*  →  resolve.spec.ts (no .svelte.) runs in the fast node project. -->
<!-- LOCKED default (Open Q#1 resolved): capable + no-preference + no-stored visitor → 'premium'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pnpm/npm tooling drift in test + e2e commands</name>
  <files>package.json, playwright.config.ts</files>
  <read_first>
    - package.json (scripts block, lines 11-25)
    - playwright.config.ts (webServer.command + the whole webServer block)
    - svelte.config.js (base = process.env.BASE_PATH ?? '' — why the E2E needs BASE_PATH set for local preview)
    - .planning/phases/03-mode-state-system-toggle/03-RESEARCH.md (Validation Architecture note: `test` script calls nested `npm run`)
  </read_first>
  <action>
    The project is pnpm-only (packageManager: pnpm@11.6.0) but two scripts shell out to `npm run`,
    which breaks consistent local/CI invocation. Fix both:
    1. In `package.json`, change the `test` script from
       `"npm run test:unit -- --run && npm run test:e2e"`
       to
       `"pnpm run test:unit -- --run && pnpm run test:e2e"`.
    2. In `playwright.config.ts`, change `webServer.command` from
       `"npm run build && npm run preview"`
       to
       `"pnpm run build && pnpm run preview"`.
    3. ALSO in `playwright.config.ts`, add an `env` to the `webServer` block so LOCAL preview matches the
       PRODUCTION base path. svelte.config.js sets `base = process.env.BASE_PATH ?? ''`, so with no env the
       base is '' and preview serves at ROOT — the subpath the Plan-03 E2E navigates would 404. Set the block to:
       `webServer: { command: 'pnpm run build && pnpm run preview', port: 4173, env: { BASE_PATH: '/diversityincludesdisability_two' } }`
    Do NOT touch any other script. Do NOT add or remove dependencies.
  </action>
  <acceptance_criteria>
    - `grep -cE '(^|[^p])npm run' package.json` returns 0 (bare `npm run` only — must NOT count `pnpm run`)
    - `grep -cE '(^|[^p])npm run' playwright.config.ts` returns 0 (bare `npm run` only)
    - `package.json` `test` script contains the exact substring `pnpm run test:unit -- --run`
    - `playwright.config.ts` webServer command contains `pnpm run build && pnpm run preview`
    - `grep -q "BASE_PATH: '/diversityincludesdisability_two'" playwright.config.ts` succeeds (preview base == prod subpath)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('./package.json').scripts.test; if(!/pnpm run test:unit -- --run/.test(t)) process.exit(1); if(/(?<!p)npm run/.test(t)) process.exit(1)"</automated>
  </verify>
  <done>No BARE `npm run` remains (only `pnpm run`) in package.json or playwright.config.ts; playwright webServer sets `BASE_PATH=/diversityincludesdisability_two` so local preview serves the prod subpath the E2E uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Precedence constants + pure resolveMode() with truth-table spec (RED→GREEN)</name>
  <files>src/lib/mode/constants.ts, src/lib/mode/resolve.ts, src/lib/mode/resolve.spec.ts</files>
  <read_first>
    - .planning/phases/03-mode-state-system-toggle/03-RESEARCH.md (Pattern 2: exact precedence algorithm; Pitfall 3: namespaced key; truth-table under Validation Architecture)
    - src/lib/content/nav.spec.ts (house spec style: vitest describe/it/expect)
    - src/lib/content/types.ts (house style: readonly types, `as const`)
    - vite.config.ts (server project include/exclude — confirms resolve.spec.ts runs in node)
  </read_first>
  <behavior>
    resolve.spec.ts (server/node project) asserts the LOCKED precedence truth table exactly:
    - stored='premium',    reducedMotion=true,  webgl=false → 'premium'      (stored wins)
    - stored='accessible', reducedMotion=false, webgl=true  → 'accessible'   (stored wins)
    - stored=null,         reducedMotion=true,  webgl=true  → 'accessible'   (MODE-03)
    - stored=null,         reducedMotion=false, webgl=false → 'accessible'   (MODE-07)
    - stored=null,         reducedMotion=false, webgl=true  → 'premium'      (LOCKED default)
    - stored='garbage',    reducedMotion=false, webgl=true  → 'premium'      (invalid stored ignored)
    - additional: STORAGE_KEY strictly equals the string 'did2:mode'
  </behavior>
  <action>
    Create `src/lib/mode/constants.ts` — the single shared constants surface:
      export const STORAGE_KEY = 'did2:mode';        // NAMESPACED — shared-origin GH Pages (Pitfall 3)
      export const DATA_ATTR = 'data-mode';
      export const MODES = ['premium', 'accessible'] as const;
      export type Mode = (typeof MODES)[number];

    Create `src/lib/mode/resolve.ts` — the SINGLE precedence source of truth (pure; signals in, mode out):
      import { STORAGE_KEY, type Mode } from './constants';
      export { STORAGE_KEY, type Mode };
      export interface ModeSignals {
        stored: string | null;         // raw localStorage value (may be junk)
        prefersReducedMotion: boolean;
        webglAvailable: boolean;
      }
      /** Precedence: explicit stored choice > reduced-motion > no-WebGL > premium default. */
      export function resolveMode(s: ModeSignals): Mode {
        if (s.stored === 'premium' || s.stored === 'accessible') return s.stored; // stored ALWAYS wins (MODE-02)
        if (s.prefersReducedMotion) return 'accessible';                          // MODE-03
        if (!s.webglAvailable) return 'accessible';                               // MODE-07
        return 'premium';                                                         // LOCKED default (Open Q#1)
      }
      export function hasWebGL(): boolean {
        try {
          const c = document.createElement('canvas');
          return !!(window.WebGLRenderingContext &&
            (c.getContext('webgl') || c.getContext('experimental-webgl')));
        } catch { return false; }
      }

    Create `src/lib/mode/resolve.spec.ts` — vitest node spec covering the 6-row truth table above plus the
    STORAGE_KEY assertion. Use `import { describe, it, expect } from 'vitest'` and a table-driven loop.
    NOTE: `hasWebGL()` touches `document`/`window` and is exercised in the browser (Plan 02); do NOT call it
    in this node spec — test only the pure `resolveMode` + `STORAGE_KEY`.

    TDD order: write resolve.spec.ts first (RED — modules absent), then constants.ts + resolve.ts (GREEN).
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run --project server src/lib/mode/resolve.spec.ts` exits 0 with ≥7 assertions
    - `grep -q "did2:mode" src/lib/mode/constants.ts` succeeds
    - `resolve.ts` contains the literal branch order: `prefersReducedMotion` line precedes `!s.webglAvailable` line precedes `return 'premium'`
    - `grep -q "return 'premium'" src/lib/mode/resolve.ts` succeeds (default is premium, not accessible)
    - resolve.spec.ts asserts the null/false/true → 'premium' row (guards the locked default)
    - No import of `three`/`@threlte` anywhere in src/lib/mode/*
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project server src/lib/mode/resolve.spec.ts</automated>
  </verify>
  <done>resolve.spec.ts is GREEN; constants.ts exports namespaced STORAGE_KEY; resolveMode encodes the locked precedence with premium default.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run --project server src/lib/mode` is GREEN (truth table).
- `pnpm check` exit 0 (types).
- No `npm run` remains in package.json / playwright.config.ts.
- src/lib/mode contains zero WebGL-rendering imports (only the cheap canvas probe).
</verification>

<success_criteria>
resolveMode is the tested single source of precedence truth with the locked default (premium); constants.ts
holds the namespaced `did2:mode` key that the store and inline script will reuse; tooling invokes pnpm.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mode-state-system-toggle/03-01-SUMMARY.md`.
</output>
